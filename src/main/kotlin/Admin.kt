class Admin(name: String, age:Int, phone:Int, admin: Boolean): User(name, age, phone, true){

     fun adminFunctions(post: String) {

         /*  fun flagPost() {

             for (i in 0..post.size - 1) {
                 var postView = post[i]
                 println("$postView ID $i")
             }
             println("Enter the Id to be flagged")
             var id = Integer.valueOf(readLine())
             var flagPost: MutableList<String> = ArrayList()
             flagPost.add("$post")
          //   homePage()
         }

    }
}



        */
     }}